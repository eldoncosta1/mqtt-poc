# URL base da API consumida pelo frontend. É embutida no bundle em BUILD-TIME —
# em produção (Dokploy) passe como Build Arg, não como variável de ambiente de runtime.
VITE_API_URL=http://localhost:3333
